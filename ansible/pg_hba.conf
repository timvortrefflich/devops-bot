local   all             postgres                                peer
local   all             all                                     peer

host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             ::1/128                 scram-sha-256

host    all             {{ db_user }}   {{ hostvars[groups['bot'][0]].ansible_host }}/32          scram-sha-256
host    replication     {{ db_repl_user }} {{ hostvars[groups['db_replica'][0]].ansible_host }}/32 scram-sha-256
